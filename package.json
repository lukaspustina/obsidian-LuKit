{
	"name": "lukit",
	"version": "1.9.0",
	"description": "Lukas' toolkit — modular workflow automations for Obsidian",
	"bin": {
		"lukit": "./cli.js"
	},
	"scripts": {
		"build": "tsc -noEmit && node esbuild.config.mjs production && node esbuild.config.mjs cli",
		"build:cli": "node esbuild.config.mjs cli",
		"dev": "node esbuild.config.mjs",
		"test": "vitest run",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.27.3",
		"obsidian": "^1.7.2",
		"typescript": "^5.6.0",
		"vitest": "^4.0.18"
	}
}
